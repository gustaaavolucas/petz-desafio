name: CI-CD

on:
  push:
    branches: ["main"]

permissions:
  contents: write

env:
  IMAGE_NAME: docker.io/${{ secrets.DOCKERHUB_USERNAME }}/hello-cicd

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # <– precisa disso pra conseguir dar pull/rebase

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        run: |
          TAG=${GITHUB_SHA::7}
          docker build -t $IMAGE_NAME:$TAG ./app
          docker push $IMAGE_NAME:$TAG
          echo "TAG=$TAG" >> $GITHUB_ENV

      - name: Update manifest with new image
        run: |
          sed -i "s|docker.io/.*/hello-cicd:.*|docker.io/${{ secrets.DOCKERHUB_USERNAME }}/hello-cicd:${TAG}|g" k8s/app-deployment.yaml

      - name: Pull latest main (avoid non-fast-forward)
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git pull --rebase origin main

      - name: Commit and push manifest changes
        run: |
          git add k8s/app-deployment.yaml
          git commit -m "ci: update image to ${TAG}" || echo "no changes"
          git push
